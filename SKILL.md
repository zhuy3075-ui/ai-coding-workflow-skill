---
name: ai-coding-workflow
description: "AI 编程项目全流程 SOP。Use when Codex must guide or execute a software project through client discovery, PRD, SPEC, UI/UX SPEC, TASKS, phase implementation, phase acceptance, debugging, MVP acceptance, delivery docs, or final delivery review. Trigger on 需求访谈, 甲方沟通, 生成 PRD, 审查 PRD, 优化 PRD, 检查 PRD 一致性, 生成 SPEC, 补充 UI SPEC, 审查 SPEC, 优化 SPEC, 最终一致性检查, 生成 TASKS, 审查 TASKS, 优化 TASKS, 自动执行当前 Phase, 阶段验收, 修复问题, 判断下一步, MVP 总验收, 生成交付文档, 最终交付验收."
---

# AI 编程项目全流程 Skill

本 skill 将《AI 编程项目标准作业手册 v2.0》压缩为 Codex 可复用工作流。核心目标是让 Codex 先做产品经理，再做架构师，再做项目拆解者，最后才写代码。

默认使用简体中文输出。只有文件名、路径、命令、代码标识符、API 名称和错误原文可以保留英文。

## 用途

- 帮助用户从模糊需求进入 `docs/PRD.md`。
- 将稳定 PRD 转换为 `docs/SPEC.md`。
- 将稳定 SPEC 拆成 `docs/TASKS.md`。
- 按 Phase 执行当前任务、验收、修复问题。
- 在所有 P0 和必要 P1 完成后进行 MVP 总验收。
- 生成交付文档并执行最终交付验收。

## 适用场景

- 新软件项目从 0 到 MVP。
- 已有项目需要补齐 PRD / SPEC / TASKS。
- 用户要求 Codex 自动执行当前 Phase，但需要防止范围失控。
- 用户要求审查 PRD、SPEC、TASKS、阶段结果或交付状态。
- 需要把 AI 编程流程沉淀成可重复 SOP。

## 触发方式

当用户出现以下意图或触发语时使用本 skill：

- `需求访谈`、`甲方沟通`
- `生成 PRD`、`审查 PRD`、`优化 PRD`、`检查 PRD 一致性`
- `生成 SPEC`、`补充 UI SPEC`、`审查 SPEC`、`优化 SPEC`
- `最终一致性检查`
- `生成 TASKS`、`审查 TASKS`、`优化 TASKS`
- `自动执行当前 Phase`、`阶段验收`
- `修复问题`、`判断下一步`
- `MVP 总验收`、`生成交付文档`、`最终交付验收`

## 必读文件

根据阶段读取这些文件：

- `docs/PRD.md`: 产品目标、用户、MVP、非目标、流程、验收。
- `docs/SPEC.md`: 架构、模块、数据结构、状态机、异常、UI、测试。
- `docs/TASKS.md`: Phase、Task、优先级、依赖、验收标准。
- `docs/PROGRESS.md`: 已完成任务、自测证据、阻塞和风险。

如果上游文档缺失，不要跳到下游阶段。例如没有稳定 PRD 时不要生成 SPEC；没有稳定 SPEC 时不要生成 TASKS；没有 TASKS 时不要自动开发。

## 安全规则

- 不跳过文档阶段。
- 不扩大 MVP。
- 不执行 PRD / SPEC / TASKS 未定义的功能。
- 不编造测试结果。
- P0 问题必须先修。
- 每个 Phase 必须验收。
- 默认每次只执行一个 Phase。
- 需要改变 `docs/TASKS.md` 边界时必须停止并报告。
- 不把后续版本功能混入当前 MVP。
- 不重构无关代码。
- 遇到产品决策、权限、凭据、生产数据、付款、隐私或高风险操作时停止并让用户确认。

## 严重程度

- P0: 项目无法启动、构建失败、核心流程不通、数据结构或状态机阻塞主流程、PRD / SPEC / TASKS 重大冲突、安全或隐私风险、交付验收失败、任务未完成却标记完成。处理方式: 停止新功能，先修复。
- P1: 异常流程不完整、UI 状态不清楚、验收标准部分缺失、日志或配置不完整、影响稳定性或维护性的非主流程问题。处理方式: 若影响 MVP 验收则先修。
- P2: 文案、视觉细节、性能小优化、非核心增强、后续版本建议。处理方式: 记录，不阻塞当前 MVP。

## 阶段总路线

1. 需求访谈与澄清。
2. 生成 PRD v1.0。
3. 审查 PRD。
4. 优化 PRD v2.0。
5. 检查 PRD 一致性。
6. 生成 SPEC v1.0。
7. 补充 UI / UX SPEC。
8. 审查 SPEC。
9. 优化 SPEC v2.0。
10. PRD / SPEC / UI 最终一致性检查。
11. 生成 TASKS v1.0。
12. 审查 TASKS。
13. 优化 TASKS v2.0。
14. 自动执行当前 Phase。
15. 阶段验收。
16. 问题定位与修复。
17. 判断下一步。
18. MVP 总验收。
19. 生成交付文档。
20. 最终交付验收。

## 命令 1: 需求访谈 / 甲方沟通

- 使用时机: 用户只有项目想法、甲方口述需求、参考产品或模糊背景，尚未形成 PRD。
- 输入要求: 项目背景、甲方目标、参考资料、约束、已知用户或场景。
- 执行步骤:
  1. 先问业务目标，不先进入技术细节。
  2. 追问目标用户、核心场景、输入、输出、权限、安全、异常、验收和交付。
  3. 重点确认 MVP 成功标准。
  4. 按优先级输出访谈提纲。
- 输出格式: `需求访谈提纲`，包含项目背景、目标用户、核心场景、MVP 范围、数据与输入、输出与结果、权限与安全、异常与边界、验收标准、参考资料。
- 停止条件: 用户无法提供业务目标或 MVP 成功标准；问题涉及敏感数据、生产权限或预算风险且未确认。
- 注意事项: 不要承诺技术方案；访谈问题必须服务后续 PRD。

## 命令 2: 生成 PRD

- 使用时机: 已有足够项目背景、访谈记录或甲方需求。
- 输入要求: 项目背景、甲方需求、访谈记录、参考资料、时间/预算/平台限制。
- 执行步骤:
  1. 创建或输出 `docs/PRD.md`。
  2. 说明为什么做、给谁用、解决什么问题、第一阶段做什么、第一阶段不做什么、什么算成功。
  3. 明确 MVP、非目标和后续版本。
  4. 信息不足时写入「假设条件」和「待确认问题」。
  5. 使用 Given / When / Then 或明确步骤写验收标准。
- 输出格式: `docs/PRD.md`，版本 `PRD v1.0`，至少包含文档信息、背景、目标、用户、场景、MVP 范围、功能需求、用户流程、产品层数据结构、输入格式、状态设计、异常、权限、非功能需求、日志反馈、验收标准、版本规划、风险、待确认问题。
- 停止条件: MVP 成功标准缺失；用户要求直接写代码；参考资料要求照抄或引入超出 MVP 的功能。
- 注意事项: PRD 不写具体技术实现，不把参考仓库复杂功能直接加入 MVP。

## 命令 3: 审查 PRD

- 使用时机: `docs/PRD.md` 已存在，进入 SPEC 前。
- 输入要求: `docs/PRD.md`。
- 执行步骤:
  1. 只审查，不修改文件。
  2. 检查 MVP 范围、用户场景、功能需求、数据结构、状态机、异常场景、验收标准和 PRD/SPEC 边界。
  3. 找出模糊、冲突、范围过大、验收不可测的问题。
  4. 按 P0 / P1 / P2 分类。
- 输出格式: `PRD 审查报告`，包含完成度评分、最大优点、最大问题、是否适合进入 SPEC、问题清单和优先优化的 5 个部分。
- 停止条件: PRD 文件不存在；PRD 与用户目标明显不一致；发现 P0。
- 注意事项: 不写代码，不把技术方案塞进 PRD。

## 命令 4: 优化 PRD

- 使用时机: PRD 审查报告指出 P0/P1 问题。
- 输入要求: `docs/PRD.md` 和 PRD 审查报告。
- 执行步骤:
  1. 可修改 `docs/PRD.md`。
  2. 将版本更新为 `PRD v2.0`。
  3. 优先修复 P0 和 P1。
  4. 补强 MVP 范围、用户流程、功能需求、数据结构、状态机、异常和验收标准。
  5. 将不确定内容标记为「待确认问题」。
- 输出格式: 修改摘要、主要新增内容、仍待确认问题、更新后的 PRD 完整内容。
- 停止条件: 需要用户做产品决策；修复会改变 MVP 边界；PRD 与甲方目标冲突。
- 注意事项: 不推翻合理原结构，不加入后续版本功能。

## 命令 5: 检查 PRD 一致性

- 使用时机: PRD v2.0 完成后，生成 SPEC 前。
- 输入要求: `docs/PRD.md`。
- 执行步骤:
  1. 不新增大段内容，只找矛盾、遗漏和不一致。
  2. 检查 MVP、功能、用户流程、状态机、异常、数据结构、输入校验、重试、权限、输出和验收标准。
  3. 检查是否有技术实现混入 PRD。
- 输出格式: `PRD 一致性检查报告`，包含问题表、必须修复问题、可后续修复问题、是否建议进入 SPEC。
- 停止条件: 发现 P0 冲突；MVP 与功能需求不一致；验收标准无法测试。
- 注意事项: 进入 SPEC 前必须处理阻塞项。

## 命令 6: 生成 SPEC

- 使用时机: PRD v2.0 稳定并通过一致性检查。
- 输入要求: `docs/PRD.md`。
- 执行步骤:
  1. 创建或输出 `docs/SPEC.md`。
  2. 严格基于 PRD，不新增产品方向。
  3. 设计 MVP 技术范围、架构、目录、模块、数据结构、状态机、核心流程、异常、接口/存储/日志、安全和验收测试。
  4. 技术不明确时列为「技术假设」。
- 输出格式: `docs/SPEC.md`，版本 `SPEC v1.0`，包含文档信息、SPEC 目标、PRD 摘要、MVP 技术范围、整体架构、目录结构、核心模块、数据结构、状态机、流程、异常处理、UI/UX、测试验收、技术假设、待确认问题。
- 停止条件: PRD 未稳定；PRD 一致性检查未通过；需要选型但风险/约束不清。
- 注意事项: SPEC 不是 PRD 复述，也不是完整代码。

## 命令 7: 补充 UI SPEC

- 使用时机: 项目有页面、表单、交互、状态展示或用户可见结果。
- 输入要求: `docs/PRD.md` 和 `docs/SPEC.md`。
- 执行步骤:
  1. 检查 SPEC 是否已有 UI / UX 设计规格。
  2. 补充页面清单、页面目标、布局、组件、操作路径、空/加载/成功/失败/禁用状态、表单校验、错误提示、结果展示、日志反馈、响应式和 UI 验收标准。
  3. 标记 PRD 未明确的界面假设。
- 输出格式: 新增或修改摘要、UI / UX 章节完整内容、是否建议单独生成 `UI_SPEC.md`。
- 停止条件: UI 需求会新增 PRD 未定义功能；高保真视觉或品牌系统需要用户输入。
- 注意事项: 不做高保真视觉稿，不指定复杂视觉风格，除非 PRD 明确要求。

## 命令 8: 审查 SPEC

- 使用时机: SPEC v1.0 完成后，生成 TASKS 前。
- 输入要求: `docs/PRD.md` 和 `docs/SPEC.md`。
- 执行步骤:
  1. 只审查，不修改文件。
  2. 检查 SPEC 是否完整承接 PRD、是否足够指导开发。
  3. 检查模块、数据结构、状态机、核心流程、异常处理、验收测试、过度设计和 MVP 越界。
  4. 按 P0 / P1 / P2 输出问题。
- 输出格式: `SPEC 审查报告`，包含评分、最大优点、最大问题、是否适合进入 TASKS、问题清单和结论。
- 停止条件: SPEC 不存在；PRD/SPEC 冲突；SPEC 加入未定义功能；核心模块无法拆任务。
- 注意事项: 不写代码，不扩大 MVP。

## 命令 9: 优化 SPEC

- 使用时机: SPEC 审查报告指出 P0/P1 问题。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md` 和 SPEC 审查报告。
- 执行步骤:
  1. 可修改 `docs/SPEC.md`。
  2. 将版本更新为 `SPEC v2.0`。
  3. 优先修复 P0 和 P1。
  4. 补清模块职责、数据结构、状态机、核心流程、异常处理和 Given / When / Then 验收测试。
- 输出格式: 修改摘要、已修复问题、仍待确认问题、是否建议进入 TASKS、更新后的 SPEC 完整内容。
- 停止条件: 优化需要改变 PRD 的 MVP；技术选型风险不清；需要产品决策。
- 注意事项: 不加入 PRD 未定义的新方向。

## 命令 10: 最终一致性检查

- 使用时机: PRD v2.0、SPEC v2.0 和 UI / UX SPEC 完成后。
- 输入要求: `docs/PRD.md` 和 `docs/SPEC.md`。
- 执行步骤:
  1. 只输出检查报告，不修改文件。
  2. 检查 UI 是否服务 PRD 的 MVP。
  3. 检查 UI 页面、组件、交互、状态、输入、输出、错误提示和验收标准是否与 SPEC 一致。
  4. 判断是否可以进入 TASKS。
- 输出格式: `PRD / SPEC / UI 最终一致性检查报告`，包含总体结论、UI 越界、模块一致性、状态机一致性、数据结构一致性、异常处理一致性、UI 验收标准、必须修复问题、最终建议。
- 停止条件: 发现 P0/P1 阻塞；UI 新增未定义功能；状态或数据流不闭环。
- 注意事项: 这一步是 TASKS 前的总门禁。

## 命令 11: 生成 TASKS

- 使用时机: PRD 和 SPEC 稳定，最终一致性检查通过。
- 输入要求: `docs/PRD.md` 和 `docs/SPEC.md`。
- 执行步骤:
  1. 创建或输出 `docs/TASKS.md`。
  2. 严格基于 PRD 和 SPEC。
  3. 按 Phase 拆成小任务。
  4. 每个 Task 标明优先级、阶段、目标、依赖、涉及文件、禁止事项、实现要点、验收标准、自测方式和完成标志。
  5. 给出 P0 总览、必要 P1 总览和推荐执行顺序。
- 输出格式: `docs/TASKS.md`，版本 `TASKS v1.0`。
- 停止条件: PRD/SPEC 不稳定；验收标准不可测；任务无法按依赖拆解。
- 注意事项: 任务要小到 Codex 可以单独完成；不要把后续版本任务混入 MVP。

## 命令 12: 审查 TASKS

- 使用时机: `docs/TASKS.md` 已生成，开发前。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md`。
- 执行步骤:
  1. 不写代码，不修改文件。
  2. 检查 TASKS 是否严格基于 PRD/SPEC。
  3. 检查越界任务、任务过大、依赖错误、遗漏任务、验收标准不清和不可单独执行的问题。
  4. 给出第一个应执行的 Task 编号。
- 输出格式: `TASKS 开发前审查报告`。
- 停止条件: 发现 P0；任务顺序无法执行；TASKS 与 SPEC 冲突。
- 注意事项: 开发前必须处理阻塞级任务拆解问题。

## 命令 13: 优化 TASKS

- 使用时机: TASKS 审查报告指出 P0/P1 问题。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md` 和 TASKS 审查报告。
- 执行步骤:
  1. 只涉及任务拆解时修改 `docs/TASKS.md`。
  2. 来源于 SPEC 模糊时先修 `docs/SPEC.md`，再同步 TASKS。
  3. 来源于 PRD/SPEC 冲突时指出冲突，并让 SPEC 服从 PRD。
  4. 修复所有 P0 和影响 MVP 验收的必要 P1。
  5. 将 TASKS 更新为 `TASKS v2.0`。
- 输出格式: `文档修复结果`，包含问题分类、修改文件、修复摘要、未修复/待确认问题、更新后的开发顺序、是否可以开始开发。
- 停止条件: 需要改变 PRD MVP；需要用户裁定任务边界；拆解后仍无法验收。
- 注意事项: 不开始写业务代码。

## 命令 14: 自动执行当前 Phase

- 使用时机: TASKS v2.0 稳定，用户明确授权执行。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md`、`docs/PROGRESS.md` 如存在，以及当前代码。
- 执行步骤:
  1. 选择当前 Phase 下一个可执行 Task。
  2. 优先级顺序为 P0、必要 P1、非必要 P1、P2。
  3. 只完成当前 Task，不做后续 Task，不改无关文件。
  4. 完成后运行可用自测: lint、typecheck、test、build、start/dev、核心流程或手动验收。
  5. 逐条对照 Task 验收标准。
  6. 更新 `docs/PROGRESS.md`。
  7. 当前 Phase 的 P0 完成后停止。
- 输出格式: 当前 Task、执行摘要、修改文件、自测结果、验收对照、PROGRESS 更新、是否进入阶段验收。
- 停止条件: 依赖未满足；需要改变 TASKS 边界；发现 P0；连续修复仍失败；需要未授权权限。
- 注意事项: 不要每完成一个 Task 就询问继续；但当前 Phase 完成后必须停。

## 命令 15: 阶段验收

- 使用时机: 当前 Phase 执行完成后。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md`、`docs/PROGRESS.md` 和当前代码状态。
- 执行步骤:
  1. 不新增功能，不继续下一个 Task。
  2. 检查本轮完成 Task、验收标准、是否越界、是否误做后续 Task、是否修改无关文件。
  3. 运行项目可用检查，不编造不存在的命令结果。
  4. 按 P0 / P1 / P2 分类发现的问题。
- 输出格式: `当前 Phase 验收报告`，包含完成任务、修改文件清单、验收标准对照、检查命令结果、发现问题、是否越界、是否可以进入下一阶段、下一步建议。
- 停止条件: 验收缺少证据；发现 P0；TASKS 与 PROGRESS 不一致。
- 注意事项: 阶段验收通过后才允许进入下一个 Phase。

## 命令 16: 修复问题

- 使用时机: 阶段验收、测试、构建、运行或用户报告发现问题。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md`、`docs/PROGRESS.md`、错误日志、验收报告或截图描述。
- 执行步骤:
  1. 不开发新功能，不执行新 Task。
  2. 分类问题: 文档不一致、TASKS/PROGRESS 不一致、任务未完成、验收未满足、构建/lint/typecheck/test 失败、UI、数据结构、状态机、异常、存储、权限、越界开发或未知 bug。
  3. 对 P0 / 必要 P1 定位根因。
  4. 制定最小修复方案。
  5. 先修 P0，再修必要 P1。
  6. 运行回归检查。
  7. 更新 `docs/PROGRESS.md`。
- 输出格式: `问题修复报告`，包含修复的问题、修改文件、修复摘要、回归检查结果、验收标准复核、未修复问题、是否可以继续。
- 停止条件: 问题需要产品决策；修复会扩大范围；连续修复仍失败；需要高风险权限。
- 注意事项: 如果问题来源于文档冲突，先修文档，再修代码。

## 命令 17: 判断下一步

- 使用时机: 用户要求 Codex 判断项目状态或继续推进。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md`、`docs/PROGRESS.md` 和当前代码状态。
- 执行步骤:
  1. 检查 TASKS 中哪些任务已完成。
  2. 检查 PROGRESS 中哪些任务已记录完成。
  3. 检查 TASKS 与 PROGRESS 是否一致。
  4. 若还有未完成 P0，继续下一个未完成 Phase 的 P0。
  5. 若 P0 完成但还有必要 P1，继续必要 P1。
  6. 若 P0 和必要 P1 都完成，进入 MVP 总验收。
  7. 若发现冲突、blocked 或待确认问题，停止并报告。
- 输出格式: 当前项目状态、选择的下一步动作、原因、执行结果或总验收计划。
- 停止条件: 文档/代码/进度不一致；存在 blocked；需要用户确认。
- 注意事项: 不要直接新增功能，不跳过验收。

## 命令 18: MVP 总验收

- 使用时机: 所有 P0 完成、所有影响 MVP 的必要 P1 完成、当前阶段验收通过、无阻塞、PROGRESS 与 TASKS 一致。
- 输入要求: `docs/PRD.md`、`docs/SPEC.md`、`docs/TASKS.md`、`docs/PROGRESS.md` 和当前代码。
- 执行步骤:
  1. 不开发新功能，不执行 P2。
  2. 检查文档一致性、任务完成度、代码检查、核心流程、UI、异常流程、安全隐私、交付准备。
  3. 运行项目可用命令。
  4. 发现 P0 / 必要 P1 时先报告，再给修复建议。
- 输出格式: `MVP 总验收报告`，结论为通过、修复 P0 后重新验收、修复 P0 + 必要 P1 后重新验收、或暂不建议交付。
- 停止条件: 仍有 P0 或必要 P1；核心流程未验证；文档状态不一致；安全风险未处理。
- 注意事项: 只允许必要小修复；需要大改时停止。

## 命令 19: 生成交付文档

- 使用时机: MVP 总验收通过或用户要求准备交付材料。
- 输入要求: 当前项目文档、代码状态、MVP 总验收报告。
- 执行步骤:
  1. 创建或更新 `README.md`、`docs/USER_GUIDE.md`、`docs/DELIVERY.md`、`docs/CHANGELOG.md`。
  2. 说明安装、启动、使用、测试、已实现功能、未实现功能、已知问题、后续建议和验收结果。
  3. 保持交付文档与 PRD / SPEC / TASKS / PROGRESS 一致。
- 输出格式: `交付文档生成结果`，包含新增/修改文件、README 摘要、USER_GUIDE 摘要、DELIVERY 摘要、CHANGELOG 摘要、是否可以正式交付。
- 停止条件: MVP 总验收未通过；交付范围不清；运行方式无法验证。
- 注意事项: 不开发新功能，不扩大 MVP。

## 命令 20: 最终交付验收

- 使用时机: 交付文档完成后，正式交付前。
- 输入要求: 当前项目所有文档、代码状态、测试/构建结果、交付文档。
- 执行步骤:
  1. 不新增功能，不执行后续版本任务。
  2. 检查 README、USER_GUIDE、DELIVERY、CHANGELOG、PRD/SPEC/TASKS/PROGRESS 一致性。
  3. 检查 P0、必要 P1、构建、测试、核心流程、已知问题、后续版本边界。
  4. 按 P0 / P1 / P2 分类问题。
- 输出格式: `最终交付验收报告`，包含交付结论、交付内容清单、MVP 功能完成情况、测试与构建结果、已知问题、后续版本建议、给甲方的交付说明。
- 停止条件: 交付文档缺失；构建或测试失败；核心流程未通过；P0 或必要 P1 未清零。
- 注意事项: 只有证据充分时才能输出“可以交付”。

## 文档模板

如需生成完整文档结构，读取 `references/document-templates.md`。

## 分发包建议

若需要给其他人分发本 skill，建议同时提供:

```text
ai-coding-workflow-skill/
├── SKILL.md
├── README.md
├── prompts/
│   ├── 01_client_interview.md
│   ├── 02_generate_prd.md
│   ├── 03_review_prd.md
│   ├── 04_optimize_prd.md
│   ├── 05_generate_spec.md
│   ├── 06_ui_spec.md
│   ├── 07_review_spec.md
│   ├── 08_generate_tasks.md
│   ├── 09_review_tasks.md
│   ├── 10_auto_execute_phase.md
│   ├── 11_phase_acceptance.md
│   ├── 12_debug_fix.md
│   ├── 13_mvp_acceptance.md
│   └── 14_final_delivery.md
└── templates/
    ├── PRD_TEMPLATE.md
    ├── SPEC_TEMPLATE.md
    ├── TASKS_TEMPLATE.md
    ├── PROGRESS_TEMPLATE.md
    └── DELIVERY_TEMPLATE.md
```
