# 生成 SPEC 提示词

请使用 `$ai-coding-workflow` 的「生成 SPEC」命令。

请读取 `docs/PRD.md`，基于当前 PRD v2.0 创建或输出 `docs/SPEC.md`。

要求：

1. 版本为 `SPEC v1.0`。
2. 严格基于 PRD，不新增产品方向。
3. 覆盖架构、模块、目录、数据结构、状态机、核心流程、异常、UI/UX、测试验收。
4. 技术不明确时写入「技术假设」。
5. 不直接写完整代码。
