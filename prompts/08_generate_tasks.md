# 生成 TASKS 提示词

请使用 `$ai-coding-workflow` 的「生成 TASKS」命令。

请读取：

- `docs/PRD.md`
- `docs/SPEC.md`

请创建或输出 `docs/TASKS.md`。

要求：

1. 版本为 `TASKS v1.0`。
2. 严格基于 PRD 和 SPEC。
3. 每个 Task 足够小、可执行、可验收。
4. 标明优先级、依赖、涉及文件、禁止事项、验收标准、自测方式和完成标志。
5. 默认按 Phase 拆解，不扩大 MVP。
