# 审查与优化 SPEC 提示词

请使用 `$ai-coding-workflow` 的「审查 SPEC」命令。

请读取：

- `docs/PRD.md`
- `docs/SPEC.md`

先不要修改文件。请检查 SPEC 是否完整承接 PRD，是否足够指导开发，是否存在模块、数据、状态机、异常、测试、UI 或 MVP 越界问题。

输出审查报告后，如果我要求继续，请再使用「优化 SPEC」命令，将版本更新为 `SPEC v2.0`，并最终执行「最终一致性检查」。
